const UiElements = {
  expandButtons: document.querySelectorAll(".expand-button"),
  areaArrows: document.querySelectorAll(".area-arrow")
};

const _ui = Object.freeze(UiElements);

export default _ui;
